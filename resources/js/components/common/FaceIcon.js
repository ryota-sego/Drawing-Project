import FaceIcon from '@material-ui/icons/Face';
import { blue } from '@material-ui/core/colors';

export default function _FaceIcon(props){
    return <FaceIcon style={{ color: blue[500], fontSize: props.size }} />
}